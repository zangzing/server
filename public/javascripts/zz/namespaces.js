// these are re-declared other places they are used
// safer to have them here too in case there are pages that don't
// declare them but that depend on javascript that tries to reference

// todo: probably a better way to set this up

var zz = zz || {};
zz.page = zz.page || {};
zz.session = zz.session || {};
zz.config = zz.config || {};
