<div class='glamour-page product-id-<%=@product.id%>'>
    <div class="product-image">
      <%= image_tag(@product.images.first.attachment(:large)) %>
    </div>

    <div class="product-name">
      <%= @product.name %>
    </div>

    <%= render "product_#{@product.id}" %>

</div>
