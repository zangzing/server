namespace :spree_zangzing do
  desc "Loads Default Values into DB"
  task :install do
    #Rake::Task['spree_zangzing:install:migrations'].invoke
    #Rake::Task['spree_zangzing:install:assets'].invoke
  end
end