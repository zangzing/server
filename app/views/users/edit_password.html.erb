<div class="viewlist" id="account-settings">
  <ul>
    <li class="item">
      <div class="header bar">
        Account Settings
        <a class="green-button" id="done-button"><span>Done</span></a>
      </div>
    </li>
  </ul>
  <ul>
    <li class="item tab add">
      <div class="title">
        <div class='section-title'>Change Password</div>
      </div>
<% form_for @change_password, :url => update_user_password_path(@user) do |f| %>

      <%= f.label :old_password %>
      <%= f.password_field :old_password %>

      <span class="field-error-message"><%= @change_password.errors.on(:old_password) unless @change_password.errors.on(:old_password).nil? %></span>
      <br/>
      <br/>


      <%= f.label :password %>
      <%= f.password_field :password %>
      <span class="field-error-message"><%= @change_password.errors.on(:password) unless @change_password.errors.on(:password).nil? %></span>
      <br/>


      <%= f.label :confirm_password %>
      <%= f.password_field :confirm_password %>
      <span class="field-error-message"><%= @change_password.errors.on(:confirm_password) unless @change_password.errors.on(:confirm_password).nil? %></span>
      <br/>


<% end %>

    </li>
  </ul>
</div>


<% content_for :page_javascript do %>
    <script>
        $(document).ready(function(){

            //done button
            $('#done-button').click(function(){
                $('form.new_users_controller_change_password').submit();
            });
        });
    </script>
<% end %>