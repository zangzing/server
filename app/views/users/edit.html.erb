
 <!-- % form_for :user, @user, :url => {:action => "update"}, :html => { :method => :put, :multipart => true } do |f| %-->
    <% form_for @user do | f|  %>
	    <%= render :partial => 'fields', :locals =>{:f => f }%>
        <div class="field">
            <%= f.label :style, "Style" %><br />
            <%= f.radio_button  :style, "gray" %>Midnight Breeze
            <%= f.radio_button  :style, "white" %>Pearly White
        </div>
	<div class="actions">
		<%= f.submit "Update"%>
	</div>

<% end %>
