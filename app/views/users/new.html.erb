<h2>Sign up</h2>

<!-- % form_for(@user) do |f| % -->
    <% form_for :user, @user, :url => {:action => "create"}, :html => { :multipart => true } do |f| %>
  	<%= render :partial => 'fields', :locals =>{:f => f }%>
	<div class="actions">
		<%= f.submit "Sign up"%>
	</div>
<% end %>

