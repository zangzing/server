<% content_for :zz_js_page_variables do %>
zz.page.is_user_homepage         =  <%= current_user?( @user )%>;
        zz.page.displayed_user_id        = '<%= @user.id %>';
        zz.page.displayed_user_base_url  = '<%= escape_javascript user_path( @user )%>';
        zz.page.displayed_user_name		 = '<%= truncate(@user.name.strip, :length=>22) %>';
        zz.page.displayed_user_pic_url   = '<%= escape_javascript @user.profile_photo_url %>';
        zz.page.displayed_user_possessive= '<%= escape_javascript @user.posessive_short_name%>';
<% end %>

<script type="text/javascript">
  zz.buy.on_before_activate(function(){
      ZZAt.track('homepage.buy.toolbar.click');
  });
</script>
