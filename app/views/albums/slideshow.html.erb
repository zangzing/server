<table class="profile">
  <tr>
    <td>
      <%= link_to  @album.name, @album %>
    </td>
    <td class="main">
      <% unless @photos.empty? %>
        <table class="photos">
	        <%= render :partial => "photos/photo_large", :collection => @photos, :as => :photo%>
        </table>
        <%= will_paginate @photos, { :class=> 'digg_pagination' } %>
      <% end %>
    </td>
  </tr>
</table>

