Photos in set:
<ul>
  <% @photos.each do |photo| %>
    <li><%= link_to photo[:name], smugmug_photo_path(:sm_album_id => params[:sm_album_id], :photo_id => photo[:id], :size => :screen) %> (ID#<%= photo[:id] %>)</li>
  <% end %>
</ul>