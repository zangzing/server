<%= form_for(@bench_test_resque_no_op) do |f| %>
  <% if @bench_test_resque_no_op.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bench_test_resque_no_op.errors.count, "error") %> prohibited this bench_test_resque_no_op from being saved:</h2>

      <ul>
      <% @bench_test_resque_no_op.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :iterations %><br />
    <%= f.text_field :iterations %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
